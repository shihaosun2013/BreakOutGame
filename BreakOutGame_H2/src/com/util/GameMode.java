package com.util;

/**
 * This ENUM is used to restrict game to specified modes. If a new mode needs to
 * be added, it should also be added in this ENUM.
 * 
 * @author shihao
 *
 */
public enum GameMode {

	PLAY, REPLAY, UNDO;
}
